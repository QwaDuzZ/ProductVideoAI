# TASK-018 — webhook-replicate: идемпотентная обработка GPU-результатов

| | |
|---|---|
| Этап | 1. API |
| Тип | backend |
| Приоритет | MVP |
| Оценка | S · 4 ч |
| Зависимости | TASK-016 |
| Ссылки | ТЗ §4.2, §3.5; FR-1507; UC-11 |

## Цель

Приём вебхуков Replicate по контракту §4.2: маппинг статусов в gpu_jobs, запуск следующих шагов, идемпотентность по prediction_id/provider_job_id.

## Что сделать

- starting/processing → processing; succeeded → done + output_url + продвижение; failed/canceled → failed + автовозврат.
- Повтор по завершённой задаче игнорируется.
- Неподписанные/без токена — отклоняются.

## Критерии приёмки (DoD)

- [ ] Дубль вебхука не создаёт повторный шаг.
- [ ] Ошибка провайдера эскалирует в failed проекта.
